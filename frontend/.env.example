# DHMS Frontend Environment Variables
#
# NOTE: The app is fully frontend-only and needs NO environment variables.
# All data is served from a built-in local data layer (seed data + localStorage).
# This file is provided for reference/documentation only.

# Optional: not required. If set, it is ignored by the local data layer.
# NEXT_PUBLIC_API_URL=
